[project]
name = "renogy-bt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "bleak",
    "configparser",
    "requests",
    "paho-mqtt",
]
