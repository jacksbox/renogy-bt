[project]
name = "energy_monitor"
version = "0.1.0"
description = "Jacksbox mobile battery monitoring"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "bleak",
    "configparser",
    "requests",
    "paho-mqtt",
]
